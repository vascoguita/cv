name: Build
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    container: texlive/texlive:latest@sha256:c8bb7e17f1391b584457701a594f1c2433c37f804f6566ad8b3f881298118846
    permissions:
      contents: write
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v6
      - name: Build PDF
        run: make
      - uses: actions/upload-artifact@v7
        with:
          path: cv.pdf
      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        with:
          files: cv.pdf
